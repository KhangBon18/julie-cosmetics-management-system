# Julie Cosmetics — Bug Registry

> Forensic Audit: 26/03/2026 | Auditor: Antigravity AI | Source of truth: Source code

## Status Legend
- **OPEN** — Bug exists, not yet fixed
- **FIXED** — Fixed in current session
- **VERIFIED** — Tested and confirmed working

---

| ID | Sev | Area | Summary | Evidence | Repro | Fix Hint | Status |
|----|-----|------|---------|----------|-------|----------|--------|
| BUG-001 | P0 | CRM | **Invoice DELETE không rollback CRM points/tier** — Trigger `trg_invoice_after_insert` chỉ fire trên INSERT. Khi xóa invoice, `total_points`, `total_spent`, `membership_tier` của customer KHÔNG được trả lại. | `schema.sql:269-285` — chỉ có `AFTER INSERT ON invoices`, không có `BEFORE DELETE ON invoices` cho CRM. | Tạo invoice cho customer → customer points tăng → xóa invoice → points GIỮU NGUYÊN. | Thêm `BEFORE DELETE ON invoices` trigger trừ `points_earned` và `final_total` khỏi customer. | OPEN |
| BUG-002 | P0 | Inventory | **Admin product UPDATE có thể ghi đè stock_quantity** — `productModel.update()` SET `stock_quantity = ?` trực tiếp từ req.body, bypass mọi trigger. Admin có thể vô tình phá tồn kho. | `productModel.js:81-85` — UPDATE SET stock_quantity = ? từ input. | PUT `/api/products/1` body `{stock_quantity: 999}` → stock nhảy từ giá trị thật sang 999. Mọi audit trail mất. | Loại `stock_quantity` khỏi UPDATE cho phép; chỉ thay đổi stock qua import/invoice. | OPEN |
| BUG-003 | P1 | Inventory | **Import DELETE không dùng transaction** — `importModel.delete()` dùng `pool.query` thay vì connection transaction. Nếu trigger xử lý xong 2/3 items rồi fail, stock bị lệch. | `importModel.js:92-94` — `pool.query('DELETE FROM import_receipts WHERE receipt_id = ?')`. Không có `getConnection()` hoặc `beginTransaction()`. | Tạo phiếu nhập 3 items → xóa → nếu trigger item 3 fail (product bị xóa), 2 items đã rollback stock nhưng item 3 không. | Wrap trong transaction, hoặc validate products exist before delete. | OPEN |
| BUG-004 | P1 | Inventory | **Invoice DELETE cũng không dùng transaction** — Tương tự BUG-003. `invoiceModel.js` delete dùng `pool.query` trực tiếp. | `invoiceModel.js:138` (cuối file) — `pool.query('DELETE FROM invoices WHERE invoice_id = ?')`. | Cascade delete invoice_items → trigger cộng stock mỗi item. Nếu 1 trigger fail, partial rollback. | Wrap trong transaction. | OPEN |
| BUG-005 | P1 | CRM | **Discount percent truyền từ FE, không verify trên BE** — `invoiceModel.create()` nhận `discount_percent` từ request body. FE tính dựa trên membership tier, nhưng BE không kiểm tra lại. Attacker có thể gửi `discount_percent: 99`. | `invoiceModel.js:73` — `const discountPct = discount_percent \|\| 0;` — dùng trực tiếp giá trị client gửi. `InvoicesPage.jsx:106-108` — client gửi `discount_percent`. | cURL POST `/api/invoices` body `{discount_percent: 50, items: [...]}` → hóa đơn giảm 50% bất kể tier. | BE phải lookup customer tier và calculate discount server-side. | OPEN |
| BUG-006 | ~~P2~~ | Auth | ~~Password change không hash new password~~ — **VERIFIED OK**: `authController.js:82-83` dùng `bcrypt.genSalt(10)` + `bcrypt.hash()` trước `updatePassword`. | `authController.js:82-83` — `const hashedPassword = await bcrypt.hash(new_password, salt)` | N/A | N/A | VERIFIED |
| BUG-007 | P2 | RBAC | **Leave GET /:id accessible cho mọi user** — Bất kỳ authenticated user nào có thể GET `/api/leaves/1` để xem đơn nghỉ của người khác. Chỉ có GET / (list) được guard bởi managerUp. | `leaveRoutes.js:7` — `router.get('/:id', leaveController.getById)` — chỉ `protect`, không check ownership. | staff01 GET `/api/leaves/1` → xem đơn nghỉ của staff02. | Thêm ownership check: `req.user.employee_id === leave.employee_id \|\| managerUp`. | OPEN |
| BUG-008 | P2 | RBAC | **Invoice GET / accessible cho mọi user** — Mọi authenticated user xem tất cả invoices. Staff01 có thể xem invoices do staff02 tạo. | `invoiceRoutes.js:6` — `router.get('/', invoiceController.getAll)` — chỉ `protect`. | Chấp nhận được nếu business rule cho phép. Đánh dấu UNVERIFIED — cần xác nhận business requirement. | Tùy business rule. | UNVERIFIED |
| BUG-009 | P2 | HR | **Salary sử dụng `position_salary` nhưng không dùng nó** — `salaryCalculation.js:13-18` query `p.base_salary as position_salary` nhưng dòng 37 dùng `emp.base_salary` (từ employees table). Nếu position base_salary khác employee base_salary, lương tính sai. | `salaryCalculation.js:13,37` — query lấy `position_salary` nhưng dùng `base_salary`. | Gán position có base_salary 15M, nhưng employee.base_salary = 10M → tính theo 10M. | Dùng `position_salary \|\| base_salary` để ưu tiên lương theo chức vụ. | OPEN |
| BUG-010 | P2 | Inventory | **Import create không validate product_id tồn tại** — `importModel.create()` insert import items mà không check product_id hợp lệ. FK constraint sẽ throw error nhưng error message không user-friendly. | `importModel.js:75-79` — insert trực tiếp, không SELECT product first. | POST create import với product_id=99999 → MySQL FK error, 500 response thay vì 400. | Validate product_id trước insert, trả message rõ ràng. | OPEN |
| BUG-011 | P2 | Security | **No input validation library** — Toàn bộ server không có express-validator, joi, yup, hoặc bất kỳ validation middleware. Tất cả req.body được trust hoàn toàn. | `grep -r "express-validator\|joi\|yup" server/` → No results. `server/package.json` dependencies kiểm tra. | POST `/api/products` body `{product_name: ""}` → product với tên rỗng được tạo. | Thêm express-validator cho routes quan trọng (invoice, salary, employee). | OPEN |
| BUG-012 | P3 | FE | **CrudPageFactory không có pagination** — `CrudPageFactory.jsx:17` gọi `service.getAll()` không params. BE trả default limit=10, nhưng FE hiển thị "X mục" header đếm từ array length thay vì total. | `CrudPageFactory.jsx:17-18` — `service.getAll()` rồi `setItems(Array.isArray(data) ? data : ...)`. | Brands có 15 items → FE hiển thị "10 mục" (array length), thiếu 5 items, không có nút next page. | Truyền pagination params, hiển thị pagination controls. | OPEN |
| BUG-013 | P3 | FE | **Staff controller hardcodes limit:100** — `staffController.js:50,83` gọi `Salary.findAll` và `Leave.findAll` với `limit: 100`. | `staffController.js:50` — `page: 1, limit: 100`. | Nếu nhân viên có >100 records lương hoặc nghỉ phép, sẽ bị cắt. | Thêm pagination param hoặc tăng limit. | OPEN |
| BUG-014 | P3 | CRM | **Review DELETE CASCADE mất data** — `reviews.customer_id` có `ON DELETE CASCADE`. Xóa customer → mất tất cả reviews. | `schema.sql:221` — `FOREIGN KEY (customer_id) REFERENCES customers(customer_id) ON DELETE CASCADE`. | Xóa customer → reviews biến mất, product rating history mất. | Đổi thành `ON DELETE SET NULL` hoặc soft delete. | OPEN |
| BUG-015 | P3 | Security | **CORS hardcoded localhost** — Production deploy sẽ fail nếu không update CORS origin. | `server.js:34` — `origin: process.env.CLIENT_URL \|\| 'http://localhost:5173'`. | Deploy server lên VPS, frontend lên Vercel → CORS block. | Cần set CLIENT_URL env var khi deploy. | OPEN |
